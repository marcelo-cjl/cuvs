一、为什么GPU让图索引构建变得不同

在向量检索领域，图索引（Graph-based Index） 已成为兼顾检索精度与查询效率的主流方案。无论是 NSW、HNSW，还是下面要探讨的 CAGRA 与 Vamana，它们的核心思想都是将高维向量空间映射为一张可导航的图结构，并通过图上的遍历与搜索来逼近真实的最近邻。
 CPU 算法追求串行高效与精细内存控制，而 GPU 以海量并行核心，用并行吞吐取代顺序优化，用冗余计算换取核心利用率。同时，有限的显存又迫使算法在设计上权衡计算与存储。下面的GPU 图构建方案CAGRA 与 Vamana来源于https://github.com/rapidsai/cuvs

二、CAGRA：迭代优化的图构建

CAGRA的核心是NN-Descent算法，这是一种基于"朋友的朋友"启发式的迭代优化方法。算法的直觉很简单：如果节点u是v的近邻，w是u的近邻，那么w很可能也是v的近邻。

NN-Descent的迭代构建流程：
1. 随机初始化：每个节点v随机选择k个其他节点作为初始邻居。
2. 本地采样：对于节点v，检查它的每个邻居u的所有邻居w，计算dist(v,w)，如果w比v当前的第k远邻居更近，就替换它。这一步是算法的核心，通过邻居的邻居来发现更好的候选。
3. 反向采样：对于v的邻居u，反向检查v是否应该成为u的邻居。如果dist(u,v)比u当前的第k远邻居更近，就将v加入u的邻居列表。
4. 收敛判断：统计每轮迭代中有多少边被更新，如果更新数量低于某个阈值（比如总边数的1%），说明图已经稳定，停止迭代。通常5-10轮迭代后就能收敛。

GPU实现的主要流程：

**本地采样阶段（Local Join）**：
1. 每个线程处理一个节点v
2. 读取v的所有邻居u，遍历每个u的邻居列表
3. 对u的每个邻居w，计算dist(v,w)
4. 维护一个优先队列，保存距离v最近的k个候选
5. 将k个最优候选写入临时缓冲区

**反向采样阶段（Reverse Join）**：
1. 每个线程处理一个节点u
2. 收集所有将u作为邻居的节点v（反向候选）
3. 对每个反向候选v，计算dist(u,v)
4. 将反向候选与u的当前邻居合并，选择最优的k个
5. 更新u的邻居列表

每轮迭代分两个阶段执行，避免多个线程同时修改同一节点时的原子操作开销。节点的邻居连续存放，内存可以合并访问。

图优化阶段：

**kern_sort**（边排序）：
将每个节点的k个邻居按距离从近到远排序。搜索时优先探索近邻居，如果连续遇到几个远邻居可以提前终止。
1. 每个thread block处理一个节点v
2. 读取v的k个邻居及其距离
3. 在共享内存中进行排序
4. 将排序后的邻居列表写回全局内存

**kern_prune**（冗余边剪枝）：
移除冗余边，使用2-hop detours机制判断边的重要性。对于边A→B（A的第k条边），检查A的前k-1个更近的邻居C，统计有多少C也连接到B（即存在2-hop路径A→C→B）。2-hop detours数量越多，说明这条边越冗余，因为可以通过其他路径间接到达B。
1. count_2hop_detours：对每条边统计2-hop detours数量
2. 对于节点A的每个邻居B，检查A的更近邻居C是否也连接到B
3. 统计每条边的2-hop detours数量，存储在detour_count数组
4. 优先保留2-hop detours数量少的边（这些边更不可替代）
5. 对每个节点选择指定数量的最重要边

**MST优化系列kernel**（最小生成树连通性优化）：
剪枝可能导致图不连通，某些节点成为"孤岛"。解决方案是构建原始图的最小生成树（MST），将MST的边强制加回剪枝后的图。MST保证全图连通性且边数最少（N-1条边）。涉及多个kernel协同工作：
1. kern_mst_opt_labeling：标记每个节点所属的连通分量
2. kern_mst_opt_cluster_size：计算每个连通分量的大小
3. 对每个连通分量，找到连接到其他分量的最短边
4. kern_mst_opt_update_graph：将这些最短边加入MST
5. kern_mst_opt_postprocessing：后处理，将MST的边合并回剪枝后的图


---

三、Vamana：插入式构建的渐进哲学

Vamana设计的动机是：对于超大规模数据集（十亿级），无法一次性在显存中构建全图。Vamana的解决方案是"化整为零"——将N个节点分成若干批次，每批插入时只需要当前批次的工作内存。

Vamana的批次大小以几何系数增长，直到达到max_batch_size，大致有三个阶段：
1. 早期小批次：当图只有几个节点时，每个新节点都能充分"探索"现有的图结构，找到最优的邻居。这些早期节点构成了图的骨架。
2. 中期批次：图已经有了一定规模，新节点可以通过图搜索找到合理的邻居。批次变大提升并行度。
3. 后期大批次：图已经成熟，新节点只是填充细节，批次大小达到上限快速完成剩余节点的插入。

每批节点的插入都执行相同的流程：GreedySearch → RobustPrune → 反向边更新

**GreedySearchKernel**（图上邻居搜索）：
新节点v要找到它的k个最近邻，但我们没有全局k-NN的先验知识。Vamana的做法是在已构建的图上进行贪婪搜索，把搜索过程中访问到的节点作为候选邻居。
1. 从中心点开始
2. 维护一个候选队列（优先队列，按距离v从近到远排序）
3. 每次从队列中取出最近的节点u，检查u的所有邻居
4. 如果邻居w之前没访问过，计算dist(v, w)，加入队列
5. 重复直到队列为空或访问了足够多的节点
6. 返回visited列表作为候选邻居

每个thread block处理一个新节点，32个线程协同维护候选队列，并行计算邻居距离。

**RobustPruneKernel**（α-遮挡剪枝）：
GreedySearch可能返回128个候选，但图的度数限制是64，需要做剪枝。Vamana剪枝算法的核心思想：α × dist(u, w) ≤ dist(v, w) 则w被u遮挡。α为松弛因子，常用值是1.2。如果u到w的距离（乘以松弛因子α）小于v到w的距离，说明从v出发通过u可以很容易到达w，保留两者是冗余的。
1. 将候选按距离排序
2. 第一个候选（最近的）总是保留
3. 对于后续每个候选w，检查它是否被已选择的任何邻居u遮挡
4. 如果没有被遮挡，保留w；否则跳过
5. 直到选够k个邻居或候选用完

这保证了邻居的方向多样性，避免所有邻居聚集在某个方向。

**反向边处理系列kernel**（双向连接）：
v选好了邻居u1, u2, ..., uk，但这些邻居并不知道v的存在。如果不处理，图是"单向"的：v→u有边，但u→v没有。这会导致搜索时从u出发无法回到v，降低图的连通性。涉及多个kernel协同工作：
1. create_reverse_edge_list：收集所有新插入的边，创建反向边列表
2. 对反向边列表按目标节点排序和分组
3. populate_reverse_list_struct：为每个受影响的节点u构建反向边候选列表
4. recompute_reverse_dists：重新计算反向边的距离
5. RobustPruneKernel：对合并后的候选列表执行剪枝，重新选择最优的k个邻居
6. write_graph_edges_kernel：将更新后的邻居列表写回图

Vamana的插入式构建看似"顺序"（一批批插入），实际上每批内部是并行的，而批间的顺序依赖无法避免（后批依赖前批构建的图）。几何增长的批次策略在并行效率和图质量之间找到平衡。

---

四、GPU并行化的共同挑战与解决

无论CAGRA还是Vamana，在GPU上实现都面临相似的挑战：

**距离计算优化**：
向量距离计算（L2或内积）是最频繁的操作。优化策略包括：指令级并行（ILP）让一个线程同时计算多个维度，Warp内归约让32个线程分别计算部分维度再用shuffle指令汇总，使用FMA（fused multiply-add）指令单周期完成乘加操作。

**共享内存管理**：
每个thread block的共享内存有限（48KB），需要存储：查询向量、候选队列、邻居数组、排序临时空间。使用union技巧让不同阶段的数据结构共享同一块共享内存，排序完成后的空间被后续的搜索数据覆盖。

**内存访问模式**：
GPU对内存访问模式敏感：连续线程访问连续地址可以合并成一次事务，否则退化成多次单独事务。图结构本身是不规则的，通过精心设计数据布局来优化：图按行主序存储保证每个节点的k个邻居连续存放，将结构体数组（AoS）转换为数组结构（SoA），ID和距离分开存储。